---
import { Icon } from "astro-icon/components";
import Prose from "../components/Prose.astro";
import Default from "../layouts/Default.astro";
---

<Default title="Halaman Tidak Ditemukan" titleAppend description="Maaf, halaman yang kamu cari tidak tersedia." seo={{ noindex: true, nofollow: true }}>
    <Prose tag="main">
        <h1 class="flex items-center gap-2">
            <Icon name={'twemoji:robot'} />
            Halaman Tidak Ditemukan
        </h1>

        <p>Maaf, halaman yang kamu cari tidak tersedia.</p>
    </Prose>
</Default>